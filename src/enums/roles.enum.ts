export enum UserRoleTitle {
  SuperAdmin = 'Super Admin',
  User = 'User',
}

export enum UserRoleType {
  SuperAdmin = 'super-admin',
  User = 'user',
}

export enum RoleMetaKye {
  SUPER_ADMIN = 'SUPER_ADMIN',
  ADMIN = 'ADMIN',
  USER = 'USER',
}
